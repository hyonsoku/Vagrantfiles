# Provisioning

```shell
$ sudo apt update
$ sudo apt upgrade
```

# VPN

```shell
$ cd /vagrant_data
$ sudo openvpn *.ovpn
```

# gobuster

```shell
$ gobuster -u {target_url} -w {dictionary_path} dir
```
